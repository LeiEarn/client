<style lang="less">
@import './style/weui.wxss';
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  box-sizing: border-box;
}
</style>

<script>
import wepy from 'wepy'
import 'wepy-async-function'
import {
  setStore
} from 'wepy-redux'
import configStore from './store'
import cookieJar from './lib/cookieJar'
const store = configStore()
setStore(store)
export default class extends wepy.app {
  config = {
    pages: [
      'pages/index',
      'pages/publish',
      'pages/authorize',
      'pages/user/mine',
      'pages/user/info',
      'pages/user/auth',
      'pages/task/task',
      'pages/task/published',
      'pages/task/accepted',
      'pages/task/detail',
      'pages/question/wjx'
    ],
    window: {
      backgroundTextStyle: 'light',
      navigationBarBackgroundColor: '#fff',
      navigationBarTitleText: 'WeChat',
      navigationBarTextStyle: 'black'
    },
    tabBar: {
      color: '#353535',
      selectedColor: '#00C000',
      list: [
        {
          pagePath: 'pages/index',
          text: '首页',
          iconPath: 'assets/index.png',
          selectedIconPath: 'assets/index.png'
        },
        {
          pagePath: 'pages/task/task',
          text: '任务',
          iconPath: 'assets/task.png',
          selectedIconPath: 'assets/task.png'
        },
        {
          pagePath: 'pages/publish',
          text: '发布',
          iconPath: 'assets/add.png',
          selectedIconPath: 'assets/add.png'
        },
        {
          pagePath: 'pages/user/mine',
          text: '我的',
          iconPath: 'assets/mine.png',
          selectedIconPath: 'assets/mine.png'
        }
      ]
    },
    navigateToMiniProgramAppIdList: [
      'wxd947200f82267e58'
    ]
  }
  globalData = {
    user: {
      userInfo: null,
      userId: null,
      hasAuthenticated: ''
    }
  }
  constructor() {
    super()
    this.use('requestfix')
  }
  onLaunch() {
    cookieJar.init()
  }
  getUserInfo(cb) {
    if (this.globalData.user.userId) {
      return this.globalData.user
    }
    const rq = require('./lib/request.js')
    let self = this
    const APP_ID = 'wx2e515a1b9f28a15e'
    const APP_SECRET = 'ded301bbd914c7a4083d15326be60073'
    wepy.login({
      success(res) {
        if (res.code) {
          var jsCode = res.code
          wepy.getUserInfo({
            success(res) {
              var object = {
                url: '/login',
                data: {
                  appid: APP_ID,
                  secret: APP_SECRET,
                  js_code: jsCode,
                  encryptedData: res.encryptedData
                },
                method: 'POST'
              }
              rq.request(object).then(res => {
                self.globalData.user.userId = res.data.userId
                self.globalData.user.userInfo = res.data.userInfo
                self.globalData.uesr.hasAuthenticated = res.data.hasAuthenticated
                self.$apply()
                cb && cb(res.data)
              }).catch(res => {
                console.log(res)
              })
            },
            fail: () => {
              console.log('你还没有授权')
            }
          })
        }
      }
    })
  }
  getAuthState(cb) {
    if (this.globalData.userId) {
      if (this.globalData.hasAuthenticated) {
        return this.globalData.hasAuthenticated
      }
      let self = this
      const rq = require('./lib/request.js')
      var object = {
        url: '/getAuthState',
        data: {
          userId: this.user.userId
        },
        method: 'GET'
      }
      rq.request(object).then(res => {
        self.globalData.hasAuthenticated = res.data.hasAuthenticated
        self.$apply()
        cb && cb(res.data.hasAuthenticated)
      }).catch(() => {
        console.log('获取认证状态失败')
      })
    }
  }
}
</script>
